#include <stdio.h>
void main()
{
    int n;
    printf("enter the number to be check");
    scanf("%d",&n);
    if (n%2==0)
    {
    printf("the number is even");
    }
    else 
    printf("the number is odd");
}